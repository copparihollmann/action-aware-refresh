# Experiment C (offline): oracle temporal refresh feasibility

Generated by `scripts/analyze_oracle_temporal.py` over **15** recorded episodes. Critical steps are contact/subtask events from the step-indexed event log, dilated by ±2 steps; the oracle is given 4 steps of lead time. No GPU was used.

| episode | steps | success | critical steps | critical rate | baseline calls | oracle (threshold) | oracle (budget-matched) | uncovered critical |
|---|---|---|---|---|---|---|---|---|
| `BlockStackingOrderAgnosticTask` | 1350 | False | 115 | 8.5% | 43 | 208 | 43 | 73 |
| `BlockStackingSpecifiedOrderTask` | 1350 | False | 40 | 3.0% | 43 | 105 | 43 | 0 |
| `BananaThenRubiksCubeTask` | 344 | True | 22 | 6.4% | 11 | 47 | 11 | 12 |
| `Stack3RubiksCubeTask` | 858 | True | 57 | 6.6% | 27 | 115 | 27 | 31 |
| `ReorientJugTask` | 291 | True | 40 | 13.7% | 10 | 69 | 10 | 31 |
| `FruitsOnPlate3Task` | 3000 | False | 1091 | 36.4% | 94 | 1419 | 94 | 994 |
| `RubiksCubeAndBananaTask` | 795 | True | 66 | 8.3% | 25 | 130 | 25 | 42 |
| `CookingClearPlateTask` | 2700 | False | 137 | 5.1% | 85 | 295 | 85 | 53 |
| `BigPumpkinInBinTask` | 900 | False | 20 | 2.2% | 29 | 62 | 29 | 0 |
| `FruitsOnPlateTask` | 4500 | False | 590 | 13.1% | 141 | 942 | 141 | 446 |
| `AppleAndYogurtInBowlTask` | 1800 | False | 115 | 6.4% | 57 | 209 | 57 | 59 |
| `BowlStackingLeftOnRightTask` | 173 | True | 18 | 10.4% | 6 | 38 | 6 | 13 |
| `BlackItemsInBinTask` | 1800 | False | 95 | 5.3% | 57 | 185 | 57 | 35 |
| `ClutterPlasticTask` | 2700 | False | 163 | 6.0% | 85 | 317 | 85 | 79 |
| `ClearOrganicObjectsTask` | 3600 | False | 854 | 23.7% | 113 | 1212 | 113 | 742 |

## The finding that reframes this experiment

- Contact-critical steps: **6.6%** of an episode (median).
- The baseline already refreshes at only **3.2%** of steps (one call per 32 control steps).
- A threshold oracle that refreshes whenever a critical moment is imminent therefore makes **4.27x** the baseline's calls (median).

> **An oracle cannot save compute by refreshing only when necessary.** Critical moments are *more* frequent than the baseline's fixed cadence, so covering them all costs more, not less. This is a structural fact about the baseline — it already reuses each action chunk for 32 control steps — and no amount of gate cleverness changes it.

So spec §11.3's ‘≥20–25% total compute reduction from temporal scheduling’ is **not reachable by refreshing less often**. The only remaining way an oracle temporal gate can earn its place is by *improving success at matched compute* — spending the same number of calls at better-chosen moments.

## Can a matched budget cover the critical moments?

In **2/15** episodes, a budget-matched oracle (exactly as many calls as the baseline) covers every critical moment with the required lead time.

In the remaining episodes the budget is the binding constraint, so a matched-budget oracle is structurally unable to be fresh at every critical moment. A negative closed-loop result there would say more about the budget than about oracle scheduling, and must be reported that way.

## What this does and does not establish

- **Established, at no GPU cost:** the direction and magnitude of the oracle's *call-count* effect, and whether a matched budget can cover critical moments. That is enough to rule out the compute-reduction framing of Experiment C.
- **Not established:** whether better placement improves task success. Only closed-loop episodes can show that, and this analysis is what decides whether to buy them.
- **Contact signal is a proxy.** There is no per-step contact-force array in the recordings; what exists is a step-indexed event log (`OBJECT_GRABBED_SUCCESS`, `TARGET_OBJECT_DROPPED`, `GRIPPER_HIT_OBJECT`, …). It is used as a proxy for contact transitions and labelled as one rather than fabricating a contact signal from object velocities.
- **These are oracles, not gates.** They read privileged state from a completed episode and are charged zero feature-extraction overhead, so they bound any learned gate from above — a learned gate must pay for flow/event computation out of whatever saving remains (spec §7).

